version: 0.2
phases:
  install:
    commands:
      - echo [+] installation started on `date`
      - npm install
  build:
    commands:
      - echo [+] Build started on `date`
      - echo "REACT_APP_GMAP_KEY=${KEY_GOOGLE_MAP}" >> .env
      - npm run build
artifacts:
  files: '**/*'
  base-directory: build